#include <stdio.h>

int main()
{
    int a,c,b;
   printf("enter no\n");
   scanf("%d",&a);
   if (a>0)
   {
       printf("it is positive");
   }
   else if (a<0)
   {
       printf("it is negative");
   }
   else if (a==0)
   {
       printf("it is zero");
   }
    return 0;
}